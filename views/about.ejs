<%- include ./part/header.ejs %>

	<% if(typeof title !== 'undefined'){ %>
		<h1><%= title %></h1>
	<% }else{ %>
		<h2>Dit is gek..</h2>
	<% }; %>

    <% if(typeof content !== 'undefined'){ %>
        <p><%= content %></p>
    <% } else { %>
        <h2>Dit is gek..</h2>
    <% }; %>

<%- include ./part/footer.ejs %>